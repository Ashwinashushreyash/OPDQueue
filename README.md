# OPDQueue
OPDQueue is basically a system to replace the token number system used in hospital. In many hospitals they use token system which is not only problematic in handling but also a inconvenient  for the patients so this system is built to solve that issue. It generates a digital token for the patient then it calculates the estimated time required.
